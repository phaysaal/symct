# sp_sqr - Squaring (compact version)
# Computes: r = a^2
# WolfSSL single-precision format

replace <sp_sqr>(sqr_a, sqr_r) by
	# Square: a * a
	pushBN sqr_a
	pushBN sqr_a
	mulBN

	# Store result
	popBN sqr_r

	return 0
end
