replace <mbedtls_mpi_random>(rnd_X, rnd_min, rnd_N, _, _) by
	print @[rnd_X, 4]
	print @[rnd_N, 4]
	@[rnd_X, 4] := @[rnd_X, 4] = 0 ? base_addr : @[rnd_X, 4]
	base_addr := base_addr + 32
	@[rnd_N, 4] := @[rnd_N, 4] = 0 ? base_addr : @[rnd_N, 4]
	base_addr := base_addr + 32
	print @[rnd_X, 4]
	print @[rnd_N, 4]
	rnd_Xp<32> := @[rnd_X, 4]
	rnd_Np<32> := @[rnd_N, 4]
	rnd_limbs<16> := @[rnd_X+6, 2]
	
	@[rnd_Xp, 32] := nondet as rnd_rnd
	@[rnd_Xp, 32] := @[rnd_Xp, 32] % (@[rnd_Np, 32] - uext256 rnd_min)
	@[rnd_Xp, 32] := @[rnd_Xp, 32] + uext256 rnd_min
	
	return 0
end
