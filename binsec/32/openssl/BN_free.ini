replace <BN_free>(_) by
        return 0
end

replace <CRYPTO_malloc>(sz) by
        ret_val<32> := base_addr
        base_addr := base_addr + sz
        return ret_val
end

replace <CRYPTO_free>(_) by
        return 0
end

