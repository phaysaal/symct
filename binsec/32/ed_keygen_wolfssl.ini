replace <__wrap_wc_RNG_GenerateBlock>(_, rnd_buf, rnd_num) by

	for rand_i<32> in 0 to (rnd_num-1) do
		@[rnd_buf+rand_i, 4] := secret
	end
        return 0

end

#replace <__GI___getrandom>(rnd_buf, rnd_num, _) by

#	for rand_i<32> in 0 to (rnd_num-1) do
#		@[rnd_buf+rand_i, 4] := secret
#	end
#        return 1

#end

#replace <getrandom>(rnd_buf, rnd_num, _) by

#	for rand_i<32> in 0 to (rnd_num-1) do
#		@[rnd_buf+rand_i, 4] := secret
#	end
#        return 1

#end
